입력 영상
lenna256.bmp (흑백 영상)
square.bmp (흑백 영상)

출력 결과
dst1 = add(src1, src2)
→ 두 영상을 단순히 더한 결과. (픽셀 단위 덧셈, 255를 넘으면 포화)
dst2 = addWeighted(src1, 0.5, src2, 0.5, 0, dst2)
→ 두 영상을 가중 평균 (여기서는 단순 평균).
dst3 = subtract(src1, src2)
→ 첫 번째 영상에서 두 번째 영상을 뺀 결과. (음수는 0으로 클램핑됨)
dst4 = absdiff(src1, src2)
→ 두 영상의 픽셀 값 차이의 절댓값.

👉 따라서 이 코드는 두 영상을 픽셀 단위로 산술 연산하여 결과를 비교 시각화하는 예제임.